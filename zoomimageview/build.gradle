apply plugin: 'com.android.library'

android {
    compileSdkVersion 30
    buildToolsVersion = '30.0.2'
    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 30
    }
}

dependencies { }